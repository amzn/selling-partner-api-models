{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "description": "The response schema for notifications sent when a tax invoice export status changes.",
  "$ref": "#/definitions/Notification",
  "definitions": {
    "Notification": {
      "type": "object",
      "required": [
        "notificationVersion",
        "notificationType",
        "payloadVersion",
        "eventTime",
        "payload",
        "notificationMetadata"
      ],
      "properties": {
        "notificationVersion": {
          "type": "string",
          "description": "The version for this notification type."
        },
        "notificationType": {
          "type": "string",
          "description": "The notification type.",
          "enum": ["TAX_INVOICE_EXPORT_STATUS_CHANGE"]
        },
        "payloadVersion": {
          "type": "string",
          "description": "The version for this payload."
        },
        "eventTime": {
          "type": "string",
          "description": "The date and time when the notification event occurred, in ISO 8601 format."
        },
        "notificationMetadata": {
          "type": "object",
          "description": "The notification's metadata, including the publish time and the application, subscription, and notification event identifiers.",
          "$ref": "#/definitions/NotificationMetadata"
        },
        "payload": {
          "type": "object",
          "description": "Event details about the change to the tax invoice export status.",
          "$ref": "#/definitions/Payload"
        }
      }
    },
    "NotificationMetadata": {
      "type": "object",
      "required": [
        "applicationId",
        "subscriptionId",
        "publishTime",
        "notificationId"
      ],
      "properties": {
        "applicationId": {
          "type": "string",
          "description": "The application identifier."
        },
        "subscriptionId": {
          "type": "string",
          "description": "The subscription identifier."
        },
        "publishTime": {
          "type": "string",
          "description": "The date and time when the notification was published, in ISO 8601 format."
        },
        "notificationId": {
          "type": "string",
          "description": "The notification identifier."
        }
      }
    },
    "Payload": {
      "type": "object",
      "required": [
        "marketplaceId",
        "exportId",
        "status"
      ],
      "properties": {
        "marketplaceId": {
          "type": "string",
          "description": "The identifier for the store where the invoice applies."
        },
        "exportId": {
          "type": "string",
          "description": "The identifier for the export request. This value is returned by the `createInvoicesExport` operation."
        },
        "status": {
          "type": "string",
          "description": "The status of the export request. Possible values: REQUESTED - The export has been received. PROCESSING - The export is being generated. DONE - The export is complete and available for download. ERROR - The export failed.",
          "enum": ["DONE", "ERROR", "REQUESTED", "PROCESSING"]
        }
      }
    }
  }
}
